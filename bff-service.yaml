apiVersion: v1
kind: Service

metadata:
  name: bff-service

spec:
  selector:
    app: bff

  type: NodePort

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080